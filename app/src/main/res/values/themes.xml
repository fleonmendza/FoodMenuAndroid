<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FoodMenu" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

    </style>

    <style name="Theme.FoodMenu" parent="Base.Theme.FoodMenu" />

    <style name="SplashScrean" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Fondo del SearchView -->
        <item name="android:background">@color/gray</item>
        <!-- Color del texto ingresado -->
        <item name="android:textColor">@color/white</item>
        <!-- Estilo del texto ingresado (puedes agregar más atributos según sea necesario) -->
<!--        <item name="android:textStyle">bold</item>-->
        <!-- Icono de búsqueda -->
        <item name="searchIcon">@drawable/ic_search</item>
        <!-- Ícono de cierre (cuando hay texto ingresado) -->
        <item name="closeIcon">@drawable/ic_close</item>
        <!-- Otros atributos según sea necesario -->
    </style>

</resources>